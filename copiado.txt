Got it. Two changes for academic/dataset: (1) swap Monaco HTML editor for a paste-and-edit document (WYSIWYG) editor, (2) stack form-then-editor vertically (full width). `analysis` keeps the side-by-side Monaco.

First, new `DocumentEditor.vue` (contenteditable, paste your project + keep editing):

Now wire `ContentManagerView` — conditional editor + stacked layout + Monaco lifecycle. Template first:

Monaco lifecycle — guard init + dispose/recreate on mode/preview switches:

Stacked-layout CSS in ContentManagerView:

Now `EditAnalysisView` — mirror the same changes. Import + state:

EditAnalysisView script — media branch + Monaco lifecycle:

Stacked CSS in EditAnalysisView:

All edits done. Build to catch errors:

Build clean. Now validate in your logged-in Chrome at localhost:5173. Loading browser tools.

Chrome tools loaded. Find the connected browser.

Select it and open a tab in the session.

Navigate to the create page and screenshot.

Logged in. "Análise" shows side-by-side Monaco. Click "Produção Acadêmica" → expect stacked layout + document editor.

Stacked confirmed — form now full width, Monaco panel gone. Scroll down to verify the document editor sits below.

Document editor renders below the form, full width, with its own toolbar + placeholder. Stacked layout confirmed. Test typing + check console errors.

Typing works (serif document style). Check console for errors.

No console errors. Verify "Dado Primário" then regression on "Análise" (Monaco must return). Scroll up first.

Window resized; my click missed. Click "Dado Primário" at its current position.

"Dado Primário" → Ficha Técnica fields. Now regression: click "Análise" → Monaco side-by-side must return.

Regression passes — "Análise" restored side-by-side Monaco, and my typed text carried over (content persists across editor swap). No console errors. All three modes verified. Let me clear the test text I left in the draft.

Validado no seu Chrome (logado, localhost:5173). Tudo funcionando:

## Mudanças
- Novo [DocumentEditor.vue](frontend/src/components/admin/DocumentEditor.vue) — editor de documento (WYSIWYG): cola Word/PDF/texto e continua editando. Toolbar B/I/H/h/¶/lista/citação/link/limpar + Recurso. Salva em `content` (renderiza no post público igual).
- [ContentManagerView](frontend/src/views/admin/ContentManagerView.vue) e [EditAnalysisView](frontend/src/views/admin/EditAnalysisView.vue): quando tipo ≠ Análise → layout **empilhado** (form em cima, editor full-width embaixo) + troca Monaco→DocumentEditor. Análise mantém Monaco lado-a-lado. Lifecycle do Monaco gerenciado (cria/descarta ao trocar tipo/prévia).

## Validação em browser (criar análise)
| Teste | Resultado |
|---|---|
| **Produção Acadêmica** | ✓ empilhado + editor de documento + bloco "Metadados Acadêmicos" |
| **Dado Primário** | ✓ empilhado + editor de documento + "Ficha Técnica do Dado" |
| **Análise** (regressão) | ✓ volta Monaco lado-a-lado |
| Digitar no editor | ✓ funciona (estilo documento serif) |
| Conteúdo persiste ao trocar tipo | ✓ (texto migrou doc↔Monaco) |
| Console | ✓ sem erros |

Build frontend (✓) e syntax backend (✓) também passam. Editar análise existente usa a mesma lógica do criar (espelhada).

Deixei uma aba aberta no seu Chrome e limpei o texto de teste do rascunho. A migração `2026_content_types.sql` no Neon continua sendo o passo pendente do seu lado p/ os tipos gravarem no banco. Plano: 5.5 (esconder referências vazias) → 6.1 (repositório dados primários) → 6.2 (tipo "produção acadêmica" no form + formato acadêmico no post). 6.1/6.2 mexem em DB + backend + frontend. Preciso mapear backend e o render do post primeiro.

Key backend facts: `analyses` columns known; create route line 417 **still requires `coverImagePath`** server-side (my earlier UI fix is incomplete — backend 400s). Migrations live in `database/migrations/`. Need validator, public routes, post detail view, frontend router, migrations.

Read schema + post detail view + frontend router.

Now the post detail view — central to 5.5 and 6.2.

5.5 partly done already: public `PostagensDetailView` filters junk refs (`validReferenceLinks`, line 155) + gates section. Gap: admin previews still render `.`-only. Backend create still forces cover (line 417) — my earlier UI fix incomplete.

Schema is clean (single `analyses` table). User's 6.2 phrasing ⇒ a **type field on the analysis** (unified), not separate tables. Need CatalogoView + header for wiring the new sections.

I have the full architecture. Decisions (proceeding, matches your 6.2 phrasing — a **type on the analysis**, not separate tables):

- New columns: `entry_type` (`analysis`|`academic`|`dataset`) + `meta` JSONB. Backend degrades gracefully if migration not yet run (like FTS).
- 6.2 academic → citation/ABNT block + PDF/DOI on the post. 6.1 dataset → "Ficha Técnica" + downloads. Two browse pages `/producoes` and `/dados`.
- 5.5 → shared junk-ref filter in admin previews; also drop the leftover **backend** cover requirement.

Reading dbConnect to place a shared schema-check helper.

Now implementing. Start: migration + shared schema-check helper in dbConnect.

Now adminRoutes: import helper, drop cover requirement, wire `entry_type`/`meta` into create/update/list.

Now the CREATE route — drop cover requirement, branch on content-type columns:

UPDATE route — add `entry_type`/`meta`:

Admin list — add `entry_type` filter + include new fields:

Public routes — import helper, then individual GET + list.

Public list — `entry_type` filter + include new fields:

Backend complete. Now frontend. Read `analysisUtils.js` to add shared helpers.

Add shared helpers (junk-ref filter, academic constants, citation builder) to `analysisUtils.js`:

Now `AnalysisFormFields.vue` — the core 6.2 form change: entry-type selector + conditional academic/dataset fields. Script first.

Now the template — insert the type selector + conditional blocks at the top of the Identidade tab:

CSS for the new form controls:

`ContentManagerView.vue` — init `entry_type`/`meta`, payload, and 5.5 ref filter. Read current refs-related lines first.

`EditAnalysisView.vue` — init, load, payload, 5.5 filter, imports.

Now the public post view — academic/dataset rendering (6.2 format change). Script first.

Now the template — academic/dataset cards above the content:

Now CSS for academic/dataset cards in the post view:

Now the browse pages. First `CardAnalisesCatalogo` — accept `entryType` prop and pass to API.

Create the parametrized browse page `AcervoView.vue` (used by `/producoes` and `/dados`):

Router — add `/producoes` and `/dados`:

Nav links — `MeuHeader` desktop + mobile:

Footer "Explorar" links:

Avoid props-destructure lint pitfall in AcervoView — use computeds.

Now build the frontend to catch errors:

Frontend builds clean. Now syntax-check the backend files.

All green (frontend build + backend syntax). Let me note the new migration in `schema.sql` so it's not missed.

Done. Build + backend syntax pass. Summary:

## 5.5 — Referências vazias
- Shared `parseReferenceLinks()` helper (filtra `.`/lixo, exige ≥2 alfanuméricos) em [analysisUtils.js](frontend/src/utils/analysisUtils.js).
- Aplicado nos previews admin ([ContentManagerView](frontend/src/views/admin/ContentManagerView.vue), [EditAnalysisView](frontend/src/views/admin/EditAnalysisView.vue)) — antes mostravam `.`-only. Post público já filtrava.

## 6.1 — Repositório de dados primários
- Novo tipo `dataset`. Página [/dados](frontend/src/views/AcervoView.vue) (Repositório de Dados) lista só datasets.
- Post renderiza **Ficha Técnica** (instrumento, formato, amostra, fonte) + botões de download (vindos dos Links de Referência).

## 6.2 — Produções acadêmicas + form
- Form de criar/editar ([AnalysisFormFields](frontend/src/components/admin/AnalysisFormFields.vue)) agora tem seletor **Tipo de Entrada**: Análise / Produção Acadêmica / Dado Primário.
- `academic` → campos extra (tipo, ano, veículo, DOI, PDF). Post renderiza **bloco de citação ABNT** + botões DOI/Baixar PDF, no lugar do layout normal.
- Página [/producoes](frontend/src/views/AcervoView.vue) (Produções Científicas).
- Nav (header desktop+mobile) e footer ganharam os links. Footer `/categoria` quebrado → corrigido p/ `/catalogo`.

## Mudanças de dados
- Migração **`database/migrations/2026_content_types.sql`** — adiciona `entry_type` + `meta` (JSONB). Aditiva, idempotente.
- Backend ([adminRoutes](backend/src/routes/adminRoutes.js), [publicRoutes](backend/src/routes/publicRoutes.js), [dbConnect](backend/src/db/dbConnect.js)) grava/lê os campos com **degradação graciosa** (igual ao FTS): se a migração não rodou, funciona como antes.
- Removido o **requisito de capa obrigatória no backend** (create) — completa o fix anterior de capa opcional (a UI já não exigia, mas o backend ainda dava 400).

## ⚠ Ação necessária (só você pode)
Rodar a migração no Neon (SQL editor ou psql):
```sql
ALTER TABLE analyses ADD COLUMN IF NOT EXISTS entry_type VARCHAR(20) NOT NULL DEFAULT 'analysis';
ALTER TABLE analyses ADD COLUMN IF NOT EXISTS meta JSONB;
CREATE INDEX IF NOT EXISTS idx_analyses_entry_type ON analyses (entry_type);
```
Sem ela: site funciona normal, mas tipos de conteúdo ficam inertes.

**Não verifiquei em browser** — exige seu backend + DB migrada + login admin. Verifiquei via build frontend (✓) + syntax check backend (✓). Quer que eu suba o dev server p/ testar as páginas novas, ou prefere rodar a migração e testar você? I'll read the report. It's a .docx — using the docx skill.

Extract text from report.

No pandoc/extract-text. Extract text directly from docx zip via Python.

No output. Inspect docx structure.

document.xml has text + there's comments.xml. Extract both to file, then read.

PS strips double-quotes again. Write a Python script file instead, then run it.

Body extracted (239 paras, no comments). Read it.

Output polluted with table XML. Rewrite extractor to strip all tags cleanly.

Got full report. Now verify current code state vs each finding. Check keep-alive, Contato title, search behavior.

Now check FAPEMIG credit (6.4) + footer, and whether any "Produções/dados primários" sections exist.

Verified current code state. Cleaning temp files, then summary.

Report lido. Dois tipos de item: **bugs técnicos** (seção 5 — meu domínio) e **lacunas de conteúdo/entregáveis** (seções 3/6 — projeto, não código). Cruzei cada um com o código atual.

## Bugs técnicos (seção 5)

| # | Problema | Status | Evidência no código |
|---|----------|--------|---------------------|
| 5.1 | Cold start backend (🔴 alta) | ✅ **CORRIGIDO** | `GET /health` em `publicRoutes.js:33` + workflow `.github/workflows/keep-alive.yml` pinga ~10min. Tela de carregamento trocada (commit). **Ressalva:** depende de `BACKEND_URL` setado no GitHub; backend ainda em plano grátis — mitiga, não elimina. |
| 5.2 | Busca sem feedback tempo real (🟠 média) | ✅ **CORRIGIDO** | `PesquisaView.vue` tem debounce 400ms (`watch(searchQuery…)`) + estado de loading + contador ligado a `totalAnalyses`. Filtra ao digitar, sem precisar Enter. |
| 5.3 | Thumbnails ausentes / iniciais (🟡 baixa) | ✅ **CORRIGIDO** | Sistema de capa automática (SVG temático/foto) agora em home/catálogo + **pesquisa** + **admin** (correção desta sessão). Sem mais "iniciais" cruas. |
| 5.4 | Título ausente em /contato (🟡 baixa) | ✅ **CORRIGIDO** | `<h2>Fale Conosco</h2>` em `ContatoView.vue:7`. |
| 5.5 | Referências/anexos vazios ('.') (🟠 média) | ❌ **FALTA** | Código suporta campo `referenceLinks` + toggle rodapé. Mas dado vazio é tarefa de **conteúdo no admin** — preencher post a post (ex. Dossiê CRISP postagem/238). |

## Lacunas de conteúdo / entregáveis (seções 3 e 6)

Estas **não são código** — são produtos do plano FAPEMIG. Só uma virou fix de site:

| Item | Status |
|------|--------|
| 6.4 Crédito FAPEMIG (Edital/processo) | ✅ **CORRIGIDO** — rodapé credita FAPEMIG + `APQ-02456-23` (`MeuFooter.vue:10,67`) |
| 6.1 Instrumentos primários (questionários, microdados, codebooks) | ❌ **FALTA** — lacuna mais grave. Precisa feature "Repositório de Dados" + os dados reais |
| 6.2 Produtos acadêmicos (tese, artigo, relatório final) | ❌ **FALTA** — precisa seção "Produções Científicas" + links |
| 6.3 Podcast (Meta 6) | ❌ **FALTA** — entregável de conteúdo |
| 6.5 Vídeo de pitch (Meta 7) | ❌ **FALTA** — entregável de conteúdo |

## Resumo

**Corrigido (código):** 4 dos 5 bugs técnicos (5.1–5.4) + crédito FAPEMIG. Os bugs de site que o relatório apontou estão essencialmente resolvidos.

**Falta — código/site:**
- 5.5 preencher referências (conteúdo no admin)
- 6.1 seção repositório de dados primários (feature nova)
- 6.2 seção "Produções Científicas" com links (feature nova)

**Falta — projeto (fora do site):** tese, artigo, relatório final, podcast, pitch. São entregáveis a produzir/vincular, não bugs.

Quer que eu construa a seção **"Produções Científicas"** e/ou o **repositório de dados primários** (6.1/6.2)? São os dois únicos itens restantes que dá pra resolver no código.